import { State } from '@/constants/variableType'

export const selectItems = (state: State) => state.selectItems
export const userInfo = (state: State) => state.userInfo
export const roleInfo = (state: State) => state.roleInfo
